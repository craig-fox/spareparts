spring:
  application:
    name: spareparts
  datasource:
    url: jdbc:h2:mem:spareparts
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
